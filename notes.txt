1. git clone {url}
2. git add notes.txt
3. git commit -m 'notes.txt'
4. git push origin main

when file added new code 
5. git pull origin main --rebase
   git push -m 'notes.txt'
   git push origin main

# afte that check status
6. git status
   - nothing to commit, working tree clean